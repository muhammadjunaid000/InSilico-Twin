# ═══════════════════════════════════════════════════════════════════
# InSilico Twin - Verified Deployment Dependencies
# ═══════════════════════════════════════════════════════════════════

# 1. Core Platform
python_version==3.10.* # Force Python 3.10 (Most stable for bio-sim)
streamlit==1.32.0               # UI Framework

# 2. The "Bridge" - Use slightly older versions to ensure compatibility
numpy==1.24.3                   # <--- CRITICAL: Numpy 1.24 works with BOTH Tellurium & SDV
pandas==2.0.3                   # Compatible with SDV 1.4+
scipy==1.10.1                   # Compatible with Tellurium

# 3. Bio-Simulation Engine
# We pin specific older versions that don't demand Numpy 2.0
tellurium==2.2.8                
libroadrunner==2.4.0            # Explicitly pin the backend engine

# 4. AI / Synthetic Data
sdv==1.4.0                      # Stable version compatible with Numpy 1.24

# 5. Visualization
plotly==5.19.0
matplotlib==3.7.1
seaborn==0.12.2

# 6. Utilities
openpyxl==3.1.2                 # For Excel export if needed
